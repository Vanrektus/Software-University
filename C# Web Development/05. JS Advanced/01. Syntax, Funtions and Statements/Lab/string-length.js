function solve(firstText, secondText, thirdText) {
    let textsTotalLength = firstText.length + secondText.length + thirdText.length;
    let averageLength = Math.floor(textsTotalLength / 3);

    console.log(textsTotalLength);
    console.log(averageLength);
}